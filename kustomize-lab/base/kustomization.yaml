apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: webapp-base

resources:
- deployment.yaml
- service.yaml

commonLabels:
  version: v1.0.0
  managed-by: kustomize

commonAnnotations:
  description: "Base webapp configuration"
