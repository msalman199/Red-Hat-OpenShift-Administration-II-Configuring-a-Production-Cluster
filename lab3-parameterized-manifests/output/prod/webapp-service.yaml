apiVersion: v1
kind: Service
metadata:
  name: webapp-service-prod
  labels:
    app: webapp
    environment: prod
spec:
  selector:
    app: webapp
    environment: prod
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
  type: LoadBalancer
