apiVersion: v1
kind: Service
metadata:
  name: redis-nodeport-svc
  labels:
    app: redis-tcp
spec:
  type: NodePort
  selector:
    app: redis-tcp
  ports:
  - port: 6379
    targetPort: 6379
    nodePort: 30379
    protocol: TCP
    name: redis-tcp
