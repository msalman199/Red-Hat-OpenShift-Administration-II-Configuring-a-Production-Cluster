apiVersion: v1
kind: Service
metadata:
  name: redis-loadbalancer-svc
  labels:
    app: redis-tcp
spec:
  type: LoadBalancer
  selector:
    app: redis-tcp
  ports:
  - port: 6379
    targetPort: 6379
    protocol: TCP
    name: redis-tcp
